<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lac.cvapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.AddCvActivity"
            android:label="@string/label_add_cv"
            />
        <activity
            android:name=".ui.AddStudyActivity"
            android:label="@string/label_add_study"
            />
        <activity
            android:name=".ui.AddDrivingLicenseActivity"
            android:label="@string/label_add_driving_license"
            />
    </application>

</manifest>